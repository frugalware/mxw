Add a new feed on the fly:

eval config.feeds.append(Rss("url", "#channel", "title"))

List the url of a feed:

eval c.privmsg("#channel", config.feeds[4].url)

Remove a feed:

eval c.privmsg("#channel", config.feeds.remove(config.feeds[4]))
